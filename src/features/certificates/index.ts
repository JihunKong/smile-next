/**
 * Certificates Feature Module
 *
 * This module contains all components, hooks, and utilities for the
 * certificate management feature including:
 * - Certificate browsing and enrollment
 * - Certificate creation and editing
 * - Certificate designer (visual editor)
 * - Progress tracking
 *
 * @example
 * ```tsx
 * import {
 *   useCertificates,
 *   useCertificate,
 *   CertificateCard,
 *   CertificateForm,
 * } from '@/features/certificates'
 * ```
 */

// Types
export * from './types'

// Hooks
export * from './hooks'

// Components
export * from './components'
