# =============================================================================
# SMILE Next.js - Environment Variables
# =============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in required values (marked with ⚠️ REQUIRED)
# 3. Ask team lead for shared API keys (see "GET FROM TEAM" notes)
# 4. Generate local secrets where indicated
#
# For a complete guide, see: docs/LOCAL_DEVELOPMENT.md
# =============================================================================

# -----------------------------------------------------------------------------
# ⚠️ REQUIRED: Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Option A: Use Docker (recommended for local dev)
#   Run: docker-compose up -d db redis
#   Then use: postgresql://smile_user:simple_pass@localhost:5432/smile_new_db
#
# Option B: Use local PostgreSQL installation
#   Create database: createdb smile_db
#   Then use: postgresql://your_user:your_password@localhost:5432/smile_db
#
DATABASE_URL="postgresql://smile_user:simple_pass@localhost:5432/smile_new_db"

# -----------------------------------------------------------------------------
# ⚠️ REQUIRED: Authentication (NextAuth.js v5)
# -----------------------------------------------------------------------------
# Generate your own secret (DO NOT share this):
#   openssl rand -base64 32
#
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_SECRET="same-as-AUTH_SECRET-above"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (OPTIONAL - needed only for Google Sign-In)
# GET FROM TEAM: Ask team lead for shared dev credentials
# Or create your own at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# AI Services (OPTIONAL for basic dev, REQUIRED for AI features)
# -----------------------------------------------------------------------------
# GET FROM TEAM: Shared development API keys
# These are costly - please use responsibly!

# OpenAI - for question evaluation
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o"

# Anthropic Claude - for Bloom's taxonomy guidance
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-4-5-20250929"

# -----------------------------------------------------------------------------
# Redis (OPTIONAL - for background job queues)
# -----------------------------------------------------------------------------
# Skip this for basic dev work. Set DISABLE_WORKERS=true instead.
# Only needed if you're working on AI evaluation features.
#
# Option A: Use Docker
#   Run: docker-compose up -d redis
#   Then use: redis://localhost:6379
#
# Option B: Use local Redis
#   brew install redis && brew services start redis
#   Then use: redis://localhost:6379
#
REDIS_URL="redis://localhost:6379"

# Set to "true" to run without Redis/workers (simpler local dev)
DISABLE_WORKERS="true"

# -----------------------------------------------------------------------------
# Email (OPTIONAL - for testing email features)
# -----------------------------------------------------------------------------
# GET FROM TEAM: Shared SMTP credentials for dev/test
# Or use a service like Mailtrap for local testing
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM="SMILE <noreply@smile.edu>"

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# -----------------------------------------------------------------------------
# OPTIONAL: Additional Services
# -----------------------------------------------------------------------------

# Stripe (for subscription features) - GET FROM TEAM if needed
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Cloudinary (for image uploads) - GET FROM TEAM if needed
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
